{% extends '::base.html.twig' %}

{% block body -%}
    <div class="container">
        {% include 'NowyBundle:Template:menu.html.twig' %}
    <h1>Lista Pakietów</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nazwa</th>
                <th>Akcje</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('package_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>
                <ul class="list-inline">
                    <li>
                        <a href="{{ path('package_show', { 'id': entity.id }) }}">Wyswietl</a>
                    </li>
                    <li>
                        <a href="{{ path('package_edit', { 'id': entity.id }) }}">Edytuj</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('package_new') }}">
                Dodaj Nowy Pakiet
            </a>
        </li>
    </ul>
    </div>
    {% endblock %}
